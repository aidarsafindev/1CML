
### `ml_for_dummies.md`

# ML для чайников: простыми словами о сложном

## Что такое Machine Learning?

Машинное обучение — это когда компьютер учится на примерах, а не по жестким правилам.

**Пример:** Вы показываете ребенку 100 картинок котиков и 100 картинок собачек. Потом показываете новую картинку, и ребенок говорит: "Это котик". Это и есть машинное обучение.

## Три основных типа задач в нашем проекте

### 1. Регрессия — предсказание чисел

**Вопрос:** "Сколько?" или "Когда?"

**Пример:** У вас есть данные, что диск рос так:
- 1 января: 100 ГБ
- 2 января: 105 ГБ
- 3 января: 111 ГБ
- 4 января: 118 ГБ

**Что делает ML:** Проводит линию через эти точки и говорит: "Через 7 дней будет 150 ГБ, через 14 дней — 180 ГБ, а через 30 дней диск заполнится полностью".

**Код для чайников:**

# Это как линейка, которой вы проводите линию через точки
from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(дни, размер_диска)
прогноз = model.predict([[30]])  # что будет через 30 дней

2. Классификация — распределение по категориям
Вопрос: "Это нормально или авария?"

Пример: Вы показываете системе много примеров:

Когда сессий было мало, а время отклика маленькое — это "НОРМА"

Когда сессий стало много, время отклика выросло, и появились блокировки — это "ДЕДЛОК"

Что делает ML: Запоминает паттерны и говорит: "Сейчас похоже на дедлок на 85%".

Код для чайников:

from sklearn.ensemble import RandomForestClassifier

model = RandomForestClassifier()
model.fit(метрики, метки_сбоев)
риск = model.predict_proba(текущие_метрики)  # вероятность сбоя

3. Обнаружение аномалий — поиск странного
Вопрос: "Что-то не так?"

Пример: Обычно в будни в 10 утра у вас 100 пользователей. А сегодня в 10 утра их 30. Это аномалия.

Что делает ML: Строит "коридор нормы" и кричит, когда что-то выходит за границы.

Код для чайников:

from sklearn.ensemble import IsolationForest

model = IsolationForest(contamination=0.05)  # ожидаем 5% аномалий
model.fit(исторические_данные)
аномалии = model.predict(новые_данные)  # -1 = аномалия, 1 = норма

Алгоритмы, которые мы используем
Линейная регрессия (Linear Regression)
Сложность: ⭐☆☆☆☆ (очень просто)

Суть: Проводим прямую линию через точки данных.

Когда использовать: Когда есть явный тренд (диск растет, время выполнения увеличивается).

Плюсы:

Очень быстрая

Понятная (можно объяснить боссу)

Работает с малым количеством данных

Минусы:

Не видит сезонность (понедельник ≠ пятница)

Не работает со сложными зависимостями

Случайный лес (Random Forest)
Сложность: ⭐⭐☆☆☆ (средне)

Суть: Как консилиум врачей. 100 разных "деревьев решений" голосуют, и мы берем среднее.

Когда использовать: Для классификации (норма/сбой) и когда много признаков.

Плюсы:

Очень точный

Не переобучается

Может определить важность признаков

Минусы:

Медленнее регрессии

Требует больше данных

Isolation Forest
Сложность: ⭐⭐☆☆☆ (средне)

Суть: Ищет то, что сложно изолировать от остальных. Аномалии — это "одинокие волки".

Когда использовать: Для поиска аномалий (падение сессий, всплески блокировок).

Плюсы:

Специально создан для аномалий

Работает быстро

Не требует размеченных данных

Prophet (от Facebook)
Сложность: ⭐☆☆☆☆ (просто, но мощно)

Суть: Учитывает сезонность — то, что в понедельник всегда больше народу, а в выходные меньше.

Когда использовать: Для прогнозов с явной периодичностью.

Плюсы:

Встроен в Grafana (3 клика!)

Учитывает праздники

Дает доверительный интервал

🔧 Как выбрать алгоритм?
Ситуация	Алгоритм	Почему
Диск растет линейно	Linear Regression	Просто, быстро, понятно
Прогноз с учетом выходных	Prophet	Умеет в сезонность
Нужно предсказать сбой	Random Forest	Учитывает много факторов
Ищем необычное поведение	Isolation Forest	Специально для аномалий
Мало данных (менее 50 точек)	Linear Regression	Простые модели работают лучше
📈 Метрики качества: как понять, что модель не врет
MAE (Mean Absolute Error)
Что это: Средняя ошибка в ГБ.
Пример: Если MAE = 5 ГБ, значит прогноз ошибается в среднем на 5 ГБ.
Хорошо: < 10% от среднего значения

R² (коэффициент детерминации)
Что это: Насколько хорошо модель объясняет данные (от 0 до 1).
Пример: R² = 0.85 означает, что модель объясняет 85% изменений.
Хорошо: > 0.7

Точность (Precision) и Полнота (Recall)
Что это: Для классификации сбоев.

Precision: Если модель сказала "сбой", то с какой вероятностью он случится

Recall: Какую долю реальных сбоев мы нашли
Хорошо: > 0.8

🚀 Практические советы
Начинайте с простого — сначала линейная регрессия, потом сложные модели

Больше данных — минимум 30 точек для обучения

Проверяйте на истории — обучите на старых данных, проверьте на новых

Доверительный интервал — прогноз без указания погрешности бесполезен

ML — не магия — если нет закономерности, ML не поможет

📚 Пример из жизни
Задача: Предсказать, когда упадет производительность отчета "Оборотка"

Шаг 1: Собираем историю — время выполнения отчета за последние 3 месяца
Шаг 2: Пробуем линейную регрессию — видим, что время растет
Шаг 3: Добавляем Prophet — учитываем, что в конце месяца всегда тяжелее
Шаг 4: Получаем прогноз: "Через 2 недели время выполнения превысит 5 минут"
Шаг 5: Успеваем оптимизировать до того, как пользователи пожалуются

Частые вопросы
Вопрос: Нужно ли быть математиком, чтобы использовать ML?
Ответ: Нет. Современные библиотеки скрывают всю математику. Нужно понимать идею.

Вопрос: Сколько нужно данных?
Ответ: Для прогноза диска — от 2 недель. Для предсказания сбоев — от 2 месяцев.

Вопрос: ML ошибается?
Ответ: Да, всегда. Поэтому мы используем доверительные интервалы и алерты только при высокой уверенности.

Вопрос: Это заменит админа?
Ответ: Нет, это инструмент для админа. Как калькулятор для бухгалтера.

Золотое правило: ML предсказывает, человек решает. Никогда не автоматизируйте действия без контроля!

